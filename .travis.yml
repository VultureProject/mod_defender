sudo: required

os: linux
dist: trusty

language: cpp
compiler: gcc

addons:
  apt:
    packages:
      - apache2
      - apache2-dev
      - g++-6
      - gcc-6
    sources:
      - ubuntu-toolchain-r-test

before_install:

install:
  - sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-6 90
  - sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-6 90

matrix:
  allow_failures:
  exclude:
    - compiler: "gcc"

  include:
    - os: linux
      compiler: "gcc"
      env: RUN="tests/travis/basic.sh"

